document.addEventListener('DOMContentLoaded', function () {
    const scroll = new LocomotiveScroll({
        el: document.querySelector('.wrp'),
        smooth: true,
        // Add other configuration options as needed
    });
});